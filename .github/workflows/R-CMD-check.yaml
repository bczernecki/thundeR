name: Build on Ubuntu
on: push

jobs:
  build:
    runs-on: bczernecki/thunder:latest
    steps:
      - name: Check out repository code
        uses: actions/checkout@v2
      - name: Install jq tool
        run: |
          sudo apt-get update
          sudo apt-get install jq
          ls -alh
          pwd
